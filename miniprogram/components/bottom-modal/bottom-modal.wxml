
<view class="modal" hidden="{{!modalShow}}">
  <view class="panel">
      <i class="iconfont icon-shanchuyitiao" bind:tap="onClose"></i>
      <!-- slot插槽  具名插槽-->
      <slot></slot>
  </view>
</view>